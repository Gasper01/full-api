{
  "builds": [
    
    {
      "src": "src/**/*",
      "use": "@vercel/static-build",
      "config": {
        "outputDirectory": "dist",
        "command": "npm run build"
      }
    },
    { 
      "src": "dist/index.js",
      "use": "npm start"
    }
  ],
  "routes": [
    { "src": "/(.*)", "dest": "/dist/$1" }
  ]
}
