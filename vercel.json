{
  "builds": [
    { 
      "src": "dist/index.js",
      "use": "npm start"
    },
    {
      "src": "src/**/*",
      "use": "@vercel/static-build",
      "config": {
        "outputDirectory": "dist",
        "command": "npm run build"
      }
    }
  ],
  "routes": [
    { "src": "/(.*)", "dest": "/dist/$1" }
  ]
}
